[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
python_files = test_*.py
addopts = -v --tb=short
markers =
    llm: integration test that calls the Ollama LLM (slow, ~40s each)
    mutating: integration test that changes DB state (self-restoring)
